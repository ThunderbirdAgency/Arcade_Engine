# Polyester Publishing workshop · set these in Vercel → Project → Settings → Environment Variables (never commit real values)
STUDIO_PASSWORD=choose-one-good-password
STUDIO_SECRET=a-long-random-string-used-to-sign-session-cookies
# Optional: "Draft with Claude" in the workshop
ANTHROPIC_API_KEY=
# Optional: "Send to workshop branch" commits stories/<slug>/ to studio/<slug> in this repo
GITHUB_TOKEN=
GITHUB_REPO=ThunderbirdAgency/Arcade_Engine
